package com.phonepe.drove.models.instance;

/**
 *
 */
public enum InstanceState {
    PENDING,
    PROVISIONING,
    STARTING,
    UNREADY,
    READY,
    HEALTHY,
    UNHEALTHY,
    STOPPING,
    STOPPED,
    FAILED,
    TERMINATED,
    UNREACHABLE,
    UNKNOWN
}
