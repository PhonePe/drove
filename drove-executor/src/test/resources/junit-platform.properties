#
# Parallel test execution DISABLED
# Tests in this module depend on the Docker engine via a shared static DockerClient
# and ContainerHelperExtension that manages container lifecycle globally.
# Running tests in parallel would cause container conflicts.
#
junit.jupiter.execution.parallel.enabled = false
