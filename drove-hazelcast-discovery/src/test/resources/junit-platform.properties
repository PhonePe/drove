#
# Parallel test execution DISABLED
# Tests use fixed WireMock port (8878), fixed Hazelcast ports (5701/5702),
# and mutate global state via System.setProperty. Cannot run in parallel.
#
junit.jupiter.execution.parallel.enabled = false
